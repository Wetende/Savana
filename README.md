# Savana

Savana is a premium coffee project with a dedicated frontend in `frontend/` and a Django backend planned next.

## Project Structure

- `frontend/`: React + Vite + JavaScript landing page
- `backend/`: Django backend planned

## Frontend

The current frontend focuses on a premium wholesale coffee landing experience.

## Getting Started

```bash
cd frontend
npm install
npm run dev
```
